# TODO

## out_labels: декларация типов — выполнено

- Сокет и тип пакета независимы; один сокет может слать разные типы.
- Дефолтный тип: `type_label` → `out_labels[0]` → имя ноды → имя класса.
- Явный тип на пакет: `self.to("socket", value, label="TypeB")`.