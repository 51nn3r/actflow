# TODO

## out_labels: декларация типов — выполнено

- Сокет и тип пакета независимы; один сокет может слать разные типы.
- Дефолтный тип (метка источника): `out_labels[0]` → `label` → имя класса (`type_label` снесён в 2c).
- Явный тип на пакет: `self.to("socket", value, label="TypeB")`; пер-линковые метки — `output_map` (2c).
- Баг с потерей аргументов конструктора (`in_labels`/`out_labels`) исправлен в заходе 1.

## Review-правки (из разбора кода) — план

Порядок: заход 1 → заход 2 (1 обязателен раньше); заход 3 независим.

### Заход 1 — механика (низкий риск) — выполнено
- [x] Баг: `Task.__init__` сохраняет `in_labels`/`out_labels`/`type_label`, не затирая class-var (`task.py`)
- [x] Типизация всех параметров, все `.py`
- [x] Docstring-однострочники на методы без описания (`executor.py`, `node.py`, `control.py`)
- [x] Имена: `Controller` → `ExecutorHandle` (+ `ctrl`/`_current_ctrl` → `handle`/`_current_handle`); `_ic`/`_oc`/`_ec` → полные

### Заход 2 — фасад Node + перевод имён

Решение: два раздельных прыжка. Прыжок 1 (`ребро → слот задачи`) на узле, прыжок 2 (`слот задачи → очередь контроллера`) внутри контроллера. Дефолт маршрутизации «свободный слот» сохраняем; выброс — только при явной карте.

#### 2a — фасад + type_label — выполнено
- [x] Фасад: узел выставляет `offer`/`poll`/`collect`/`dispatch`, исполнитель ходит только через них (`node.py`, `executor.py`)
- [x] `type_label` из `OutputController` наверх: узел держит `source_label`, штампует в `dispatch` (`control.py`, `task.py`, `node.py`)

#### 2b — перевод имён (маршрутизация меняется) — выполнено
- [x] Прыжок 1 на узле: `ребро → слот задачи`; дефолт = текущий routing (совпадение/свободный слот), явная карта → выброс неучтённых (`node.py`, `task.py`)
- [x] Прыжок 2 в контроллере: `слот задачи → своя очередь`, авто 1 к 1 или карта при создании (`control.py`)
- [x] `on_dropped(packet)` вместо молчаливого сброса, дефолт no-op (`node.py`)
- [x] Fail-loud валидация (из review): значения `input_map` ⊆ слоты (`task.py`), `slot_map` биективен (`control.py`)
- Прим.: ключи `input_map` — это метки-источники (`packet.label`), не имена линков. Опечатка ключа не валидируема (произвольная метка апстрима) → ловится через `on_dropped`.

#### 2c — консолидация имён — выполнено
- [x] `input_map`/`output_map`/`on_dropped` — конфиг на Task; Node читает `self.task`, своих полей карт не держит (`node.py`, `task.py`)
- [x] `output_map: {линк → метка}` симметрично `input_map`, штампует в `dispatch` (дефолт — `source_label`); валидации нет (линки задаются позже, опечатка = мягкий mislabel)
- [x] Снесён `type_label` (дублировал `label`); цепочка → `out_labels[0] → label → classname`
- [x] SPEC.md/SPEC.ru.md: строка резолва ярлыка поправлена

### Бэклог (из review, требует решения)
- Повторный `__call__()` на одном Task с *кастомным* контроллером отдаёт всем нодам один экземпляр → общее stateful-состояние очередей течёт между нодами. Дефолтный путь безопасен (свежий контроллер на вызов). PRE-EXISTING, в примерах не стреляет. Решить: фабрика контроллера vs экземпляр.
- `get_type_hints(TaskResult)` бросает `NameError` (forward-ref `Node` без рантайм-импорта; безусловный импорт = кольцо). Библиотека не вызывает — оставлено как есть.

### Заход 3 — планировщик (независим) — выполнено
- [x] `_ready` + `_ready_set` → `ReadyQueue` (dict как упорядоченное множество, O(1) дедуп) (`executor.py`)
- [x] Выделить `WaitSet` (ждущие + дедлайны) (`executor.py`)

### Проверка — выполнено
- [x] `examples/01,02,03` — вывод и `snapshot` как раньше; 03: `упорядочен: True`
- [x] Smoke: `Worker(out_labels=("X",))()` реально меняет метку источника
- [x] `smoke_2b`: оба прыжка перевода, drop, fail-loud валидация
- [x] `py_compile` всех модулей, хвостовой перевод строки во всех `.py`

### Сделано
- [x] `BatchInput` → `BatchInputController` (пример 02)

### Review-раунд 2 (adversarial, полное состояние) — выполнено
- [x] `slot_map`: валидация покрытия ключей = слоты, частичная/лишняя карта ловится на build (`control.py`)
- [x] `offer`/`poll` контроллеров ввода: тип `Verdict` вместо `Ready | Wait` (реально возвращается `WaitUntil`) (`control.py`)
- [x] SPEC (оба языка): убран несуществующий `OrderedOutputController`; «ярлык источника внутри execute» (аксессора нет)
- [x] docstring класса `Task` ужат до 2 строк (`task.py`)
- Отклонено adversarial-проверкой (намеренное/вырожденное): dict-ветка минует emit/mark, KeyError на непроброшенный линк, `input_map={}`, фантом `"in"`, длина module-docstring, иллюстративный Terminal в README